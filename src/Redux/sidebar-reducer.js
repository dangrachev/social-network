let initialState = {
    friendsList: [
        {id: 0, name: 'Woody'},
        {id: 1, name: 'Alex'},
        {id: 2, name: 'Rick'},
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;